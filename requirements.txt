# Azure Functions runtime
azure-functions==1.*

# Azure SDK – Table Storage
azure-data-tables==12.*

# Azure SDK – identity (Managed Identity / DefaultAzureCredential / app registration)
azure-identity==1.*

# HTTP client for Microsoft Graph API calls (including Mail.Send)
requests==2.*
